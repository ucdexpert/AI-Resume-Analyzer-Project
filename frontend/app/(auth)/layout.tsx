export const metadata = {
  title: 'Login — SkillSense',
  description: 'Login to access your resume analysis and career tools.',
};

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
